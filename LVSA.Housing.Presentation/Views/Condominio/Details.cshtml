@model LVSA.Housing.Application.ViewModels.CondominioViewModel

@{
    ViewBag.Title = "Condomínio";
    ViewBag.Description = "Detalhes do condomínio";
}

<div>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Nome)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Nome)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Descricao)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Descricao)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Ativo)
        </dt>

        <dd>
            @(Model.Ativo ? "Sim" : "Não")
        </dd>

    </dl>
</div>

<div class="form-actions no-color">
    <div>
        <a href="@Url.Action("Edit")" class="btn btn-warning"><i class="fa fa-pencil"></i> Editar</a>
    </div>
</div>

